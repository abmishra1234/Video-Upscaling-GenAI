dependency:
1. first run the requirements.txt using
pip install -r requirements.txt

2. Approach: 
This script reads a low-resolution video, upscales each frame using cubic 
interpolation (for better quality), and writes the upscaled frames 
to a new video file.

3. Design & Architecture:

